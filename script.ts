let myName: string = 'Chris'
let myAge: number = 34

let anything
anything = 13

function controlMe(isTrue: boolean, somethingElse: boolean) {
    let result: number
    if (isTrue) {
        result = 12
    }
    return result
}